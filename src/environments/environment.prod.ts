// Objeto environment que contiene las variables de entorno para la configuración de la aplicación.
export const environment = {
  production: true, // Indica si la aplicación se encuentra en el entorno de producción.
  baseUrl: 'https://release-chatcompanion-1.onrender.com', // URL base de la API en producción.
  // baseUrl: 'http://localhost:3000', // URL base de la API en desarrollo (comentada para usar la de producción).
};
